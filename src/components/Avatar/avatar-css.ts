import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  avatar: {
    width: 60,
    height: 60,
    borderWidth: 3,
    borderRadius: 100,
    borderColor: '#fff',
  },
});

export default styles;
